using UnityEngine;

public class CharacterMoveHV : MonoBehaviour
{
  /*  protected overide void OnHorzontal()
    {
        hInput = Input.GetAxis("Horizontal") * moveSpeed.value;
        movement.Set(vInput, yVar, hInput);
    }
    */
}
